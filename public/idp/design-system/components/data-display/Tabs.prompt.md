Underline tab bar, as on the Home dashboard (Company / Individual). Active tab text + underline turn primary blue. Items can carry an icon and a count pill.

```jsx
<Tabs tabs={[{value:'company',label:'Company'},{value:'individu',label:'Individual'}]} onChange={setTab} />
```
