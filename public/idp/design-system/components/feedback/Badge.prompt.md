Compact status pill. Use `light` for quiet status chips, `filled` for emphasis, `gradient` + `notch` for the brand BETA tag. Color by semantic meaning.

```jsx
<Badge color="success" leftDot>Active</Badge>
<Badge color="warning">On Leave</Badge>
<Badge gradient notch>BETA</Badge>
```
